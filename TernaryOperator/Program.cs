string weather = "Cold";

string plan = weather == "Warm" ? "Go for a walk" : "Play the game at home";
Console.WriteLine(plan);

//------------------------------------------------------------------------

bool isWeekend = true;

string anotherPlan = isWeekend ? "Sleep a bit more" : "Need to wake up";

//------------------------------------------------------------------------